[package]
name = "todo-app-rs"
version = "0.1.0"
edition = "2024"

[lib]
name = "todo_app_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29.1", features = ["extension-module"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
