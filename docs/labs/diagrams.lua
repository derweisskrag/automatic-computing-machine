return {
    doubly_linked_list_issue = {
        -- Removed "Figure 1.3:" and swapped [@] for \cite{}
        caption = "Visual representation of a type mismatch where a SinglyNode is incorrectly inserted into a DoublyLinkedList, breaking the backward traversal chain (Matsakis & Klock, 2014).",
        path = "plots/doubly_linked_list.png"
    }
}